syntax = "proto3";
package flowmodus;

// Gossip protocol messages for state transition broadcast
message GossipMessage {
  string instance_id = 1;
  int64 timestamp_unix = 2;
  string message_type = 3;
  string target_id = 4;
  string payload = 5;
}
